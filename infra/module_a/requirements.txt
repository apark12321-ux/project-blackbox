# ─── Project Blackbox — Module A: Dependencies ───
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
sqlalchemy==2.0.35
asyncpg==0.30.0           # PostgreSQL async driver
httpx==0.27.0              # Async HTTP client
celery==5.4.0              # Task queue
redis==5.1.0               # Celery broker
# python-dotenv>=1.2.2 fixes GHSA-mf9w-mj56-hr94 / CVE-2026-28684 (symlink attack in set_key/unset_key; all <1.2.2 are vulnerable)
python-dotenv>=1.2.2

# Google Trends (비공식)
pytrends==4.9.3

# Development
# pytest>=9.0.3 fixes GHSA-6w46-j5rx-g56g / CVE-2025-71176 (unsafe /tmp dir handling; all <9.0.3 are vulnerable)
pytest>=9.0.3
pytest-asyncio==0.24.0
