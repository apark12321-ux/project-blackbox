# project-blackbox

NuTube 사이트 저장소.

실제 소스 코드는 `artifacts/nutube/` 폴더 안에 있습니다.

## Vercel 배포 설정
- Root Directory: `artifacts/nutube`
- Framework: Vite
- Build Command: `npm run build`
- Output Directory: `dist`
