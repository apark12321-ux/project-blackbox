============================================================
v19 - JSON CMS + REST API 적용
============================================================

박 대표님 GitHub 적용 시 추가 패키지 1개 필요:

frontend/package.json 의 dependencies 에 추가:

"react-markdown": "^10.1.0"

방법 1) GitHub 에서 package.json 직접 편집:
  "dependencies": {
    ...,
    "react-markdown": "^10.1.0"   <-- 이 줄 추가
  }

방법 2) 또는 Vercel 이 자동으로 설치 안 되면 어시스턴트에게 알려주세요

